# Camada `pages` (alias → `page-views/`)

A composição de páginas TIP vive em **`../page-views/`**.

Next.js reserva `src/pages/` para o Pages Router; por isso usamos pasta física `page-views/` com alias TypeScript `@/pages/*`.

Ver `page-views/README.md` para o registro de rotas.
